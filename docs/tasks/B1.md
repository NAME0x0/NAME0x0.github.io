# Task B1 — Typed content layer, design tokens, fonts, root layout

Context: Next.js 14 App Router + TypeScript strict + Tailwind 3. Work on current branch. Do NOT touch `app/page.tsx`, `components/`, `lib/data/`, `lib/github/`, `lib/scene/` — the old site must keep compiling; it gets replaced in later tasks.

## 1. Design tokens — tailwind.config.ts
Extend (don't remove existing keys unless they conflict) `colors` with:
`void #000000, ink #E8E4DE, dim #8A8578, faint #3A3832, bone #C4B5A0, ember #D08C5A, signal #E3B341, paper #F2EDE4, soot #1C1A17`.
Font families: `display` → `var(--font-space-grotesk)`, `body` → `var(--font-manrope)`, `mono` → `var(--font-geist-mono)`.

## 2. Fonts — app/layout.tsx
next/font/google: Space Grotesk (500, 700) as `--font-space-grotesk`; Manrope (400, 600) as `--font-manrope`. Geist Mono from installed `geist` package (`GeistMono.variable` → `--font-geist-mono`). Subset latin. All three variables on `<html>`. Keep existing children/analytics wiring intact.

## 3. Root metadata + JSON-LD — app/layout.tsx
- title default `"Muhammad Afsah Mumtaz — NAME0x0"`; template `"%s · Muhammad Afsah Mumtaz — NAME0x0"`.
- description: `"Systems & ML engineer. I build machines that think, on hardware that shouldn't be able to."`
- metadataBase `https://name0x0.vercel.app`.
- JSON-LD Person `<script type="application/ld+json">`: name "Muhammad Afsah Mumtaz", alternateName "NAME0x0", email "mailto:m.afsah.279@gmail.com", jobTitle "Systems & ML Engineer", address Dubai UAE, alumniOf "Middlesex University Dubai", sameAs: ["https://github.com/NAME0x0", "https://www.linkedin.com/in/muhammad-afsah-mumtaz/", "https://x.com/NAME0X0_0", "https://huggingface.co/NAME0x0"]. Serialize with `JSON.stringify` from a typed const, not a hand-written string.

## 4. Typed content layer — lib/content/schema.ts + content/
Zod schemas (zod installed).

`ProjectSchema`: slug, name, tagline (one sentence), status (enum: `MEASURED | LIVE | SHIPPED | SHIPPED (MVP) | SPEC / IN PROGRESS`), tier (1|2), stack (string[]), links ({ repo, demo?, adapter?, gist? }), summary (2–4 sentences), metrics (array of { label, value, verified: boolean }), chapter (number 0–7). Tier-1 additionally: problem, constraints, architecture (paragraph strings; placeholder allowed ONLY as empty string ""), warStories (array, empty for now), framingRules (string[], optional).

`content/projects.ts` — zod-validated array with EXACTLY these entries. Facts are ground truth: do not embellish, do not add adjectives.

### TIER 1
**ava** — "AVA", MEASURED, chapter 3, stack [Python, PyTorch, PEFT/QLoRA, Triton, GGUF], repo https://github.com/NAME0x0/AVA.
Tagline: "A 2B model fine-tuned to beat Llama 3.2 3B on ARC — with a 42 MB adapter on a 4 GB GPU."
Summary: research/training stack for a tool-using, memory-aware assistant targeting 4 GB VRAM (RTX A2000 laptop). 17-benchmark, 16,872-task eval harness. Custom Triton kernel work, verifier-RL, external memory, published HuggingFace adapter.
Metrics (all verified: true): ARC-Challenge "82.0%", ARC-Easy "92.0%", Llama 3.2 3B-Instruct baseline "78.6%", Adapter size "42 MB", GPU "4 GB VRAM", Eval harness "17 benchmarks / 16,872 tasks".

**pantheon-trades** — "Pantheon-Trades", LIVE, chapter 4, stack [Python, FastAPI, Solidity, Foundry, Next.js], repo https://github.com/NAME0x0/Pantheon-Trades, demo https://pantheon-trades.vercel.app.
Tagline: "Eleven AI agents deliberate every prediction-market trade; refusals are recorded on-chain."
Summary: multi-agent deliberation + calibration + on-chain accountability infrastructure on Arc Testnet / Polymarket CLOB. Half-Kelly sizing, Platt/isotonic calibration, public /methodology, /council and /counter-evidence pages, 11-provider LLM fallback chain.
framingRules: ["Never describe as a trading bot", "Frame as deliberation + calibration + on-chain accountability infrastructure"].
Metrics (verified: true): Brier score "0.149 (200-market backtest)", Python tests "714", Solidity tests "65", Agents "11".

**omni** — "OMNI / PERSPECTIVE v2", SPEC / IN PROGRESS, chapter 5, stack [Rust], repo https://github.com/NAME0x0/OMNI.
Tagline: "Architecture research targeting a 1.05T-parameter sparse MoE on consumer hardware."
Summary: research + in-progress implementation TARGETING 4 GB VRAM + 32 GB RAM via layer streaming, ternary {-1,0,+1} weights, O(1) perspective-decay recurrence, top-1 routing over a 3D torus manifold (8×4×4 expert lattice), holographic distributed memory, safety polytope projection. Throughput figures are projected by bandwidth model, not measured.
framingRules: ["Always 'targeting' / 'designed to', never 'runs'", "Persistent label: RESEARCH IN PROGRESS — projections, not measurements"].
Metrics: Tests passing "243" (verified: true), Parameters "1.05T (design target)" (verified: false), Expert lattice "8×4×4 on 3D torus" (verified: true).

**agi-ledger** — "AGI-Ledger", LIVE, chapter 6, stack [TypeScript, Next.js, R3F/Three, Tailwind v4, Lenis, Framer Motion], repo https://github.com/NAME0x0/AGI-Ledger, demo https://agi-ledger.vercel.app.
Tagline: "A version-controlled observatory of human belief about AGI."
Summary: each take is a star in a 3D belief-space; ~22 seeded takes from named AI figures; every take answers "what would change my mind?".
Metrics (verified: true): Seeded takes "~22".

### TIER 2 (tagline + repo link only)
- mald — "MALD", SHIPPED, chapter 1, Rust, repo https://github.com/NAME0x0/MALD. "Single-binary local PKM: SQLite FTS5 + HNSW hybrid search, cited RAG chat to exact file/line via Ollama."
- pane — "pane", SHIPPED (MVP), chapter 1, Rust, repo https://github.com/NAME0x0/pane. "Real Linux desktops on Windows via WSL2/XRDP from one control surface; Arch-first MVP shipped as pane.exe."
- neural-nets — "neural-nets", SHIPPED, chapter 3, TypeScript, repo https://github.com/NAME0x0/neural-nets. "Six-chapter interactive in-browser curriculum: linear algebra → calculus → backprop."
- webdesk — "WebDesk", SHIPPED, chapter 2, C#/.NET 8 + WebView2, repo https://github.com/NAME0x0/WebDesk. "Any website, video, or shader as a live Windows wallpaper."
- mavis — "MAVIS", SPEC / IN PROGRESS, chapter 1, Rust, repo https://github.com/NAME0x0/MAVIS. "Windows shell environment — spec phase."
- terminus — "Terminus", SPEC / IN PROGRESS, chapter 1, C++, repo https://github.com/NAME0x0/Terminus. "Terminal environment for Windows — spec phase."
- tangled — "Tangled", SHIPPED, chapter 7, JavaScript, repo https://github.com/NAME0x0/Tangled. "Quantum-entanglement-between-browser-windows toy."

### content/identity.ts (zod-validated IdentitySchema)
name "Muhammad Afsah Mumtaz", handle "NAME0x0", lockup "Muhammad Afsah Mumtaz — NAME0x0", positioning "I build machines that think, on hardware that shouldn't be able to.", role "Systems & ML engineer", location "Dubai, UAE", visa "UAE Golden Visa — no sponsorship needed", education "BSc (Hons) IT, Middlesex University Dubai", email "m.afsah.279@gmail.com", openTo: ["AI/ML/software engineering internships and junior roles (Dubai/Abu Dhabi or remote)", "Freelance / contract web work"], affiliations: ["Chairman, MDX Computing Society", "Committee Chair, BCS Student Chapter", "2× hackathon runner-up (Odoo Hackathon Dubai; Curtin University Dubai, Nov 2025)"], openSource: ["FluentFlyout auto-update system", "HAGI research contributor"], socials { github, linkedin, x, huggingface } (URLs as in JSON-LD).

### content/now.md + lib/content/now.ts
`content/now.md` with YAML frontmatter `{ updated: "2026-07-03", building: ["The Machine — this site's rebuild"], reading: [], playing: ["Trackmania"] }` and a one-line body. `lib/content/now.ts` parses + zod-validates it (hand-write a tiny frontmatter parser; do NOT add deps).

## Hard constraints
- BANNED anywhere in new files: "sovereign", "secure enclave", "SYS.INIT", "architect" as self-title (word must not appear in identity/role strings).
- OMNI copy must never say it "runs" the model.
- No new npm dependencies.
- Finish with `npx tsc --noEmit` and `npm run lint`; fix all errors introduced by your files.
- Report files created + typecheck/lint results.
