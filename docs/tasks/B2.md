# Task B2 — Routes, editorial homepage, old-surface removal

Context: continues B1. The content layer at `content/` + `lib/content/schema.ts` is the ONLY source of copy — no copy hardcoded in JSX beyond microcopy (nav labels, section overlines). All new pages are **server components** (no "use client" except where interactivity is unavoidable — there should be none in this task). Every page must render complete HTML with JS disabled.

Design language: bg `void`, text `ink`, secondary `dim`, borders `faint`, links/accents `bone`, verified metric values `signal`, warnings/spec labels `ember`. Headings font-display, body font-body, overlines + data font-mono. Section overlines in Geist Mono terminal style: `// WORK`, `// PROOF`, `// IDENTITY`, etc. Generous black space; content column max-w-[68ch] for prose. Focus-visible rings in `bone` on ALL interactive elements. Semantic landmarks (header/nav/main/footer), skip-to-content link as first focusable element.

## 1. Shared components — components/site/
- `Header.tsx`: lockup "Muhammad Afsah Mumtaz — NAME0x0" (identity.lockup) linking to `/`; nav: Work, Writing, About, Now, CV. Server component.
- `Footer.tsx`: lockup, email (lightly obfuscated but clickable: render `m.afsah.279 [at] gmail.com` text with an `mailto:` href assembled from parts in the component — no plain-text address in one string in the HTML source), GitHub / LinkedIn / X / HuggingFace links (all external links `rel="noopener noreferrer" target="_blank"`), line "Dubai, UAE · UAE Golden Visa — no sponsorship needed".
- `StatusBadge.tsx`: renders ProjectStatus. MEASURED → signal border/text; LIVE → bone; SHIPPED/SHIPPED (MVP) → dim; SPEC / IN PROGRESS → ember, always accompanied by title attr "honestly labeled".
- `MetricsTable.tsx`: takes metrics[]; value cells in font-mono; verified metrics get `text-signal`, unverified get `text-dim` plus a "projected" annotation. Rule: signal color ONLY on verified: true.

## 2. Homepage — app/page.tsx (REPLACE the old client page entirely)
Server-rendered editorial narrative — this is the film's content fallback and the interim homepage until Phase D adds the canvas. Structure, in order (each a `<section>` with an id and mono overline):
0. `#ignition` — h1 = identity.lockup, positioning line, location + visa line, two CTAs: "See the proof" → /work/ava, "Start a conversation" → mailto (obfuscated pattern as in Footer).
1. `#metal` — overline `// METAL`. One-line rows for chapter-1 projects (mald, pane, mavis, terminus) from content: name + StatusBadge + tagline + repo link.
2. `#voice` — overline `// VOICE`. One-line rows for webdesk. Brief line that the interface layer is where machines meet people (keep to one sentence of microcopy).
3. `#mind` — overline `// PROOF`. AVA: tagline, summary, MetricsTable, link → /work/ava. Also neural-nets row.
4. `#council` — overline `// COUNCIL`. Pantheon: tagline, summary, MetricsTable, LIVE badge, links → /work/pantheon-trades + demo.
5. `#blueprint` — overline `// BLUEPRINT`. OMNI: persistent visible label "RESEARCH IN PROGRESS — projections, not measurements" (ember), tagline, summary, MetricsTable, link → /work/omni.
6. `#light` — overline `// LIGHT`. AGI-Ledger: tagline, summary, links → /work/agi-ledger + demo.
7. `#human` — overline `// HUMAN`. **Inverted section: bg-paper text-soot.** identity.affiliations, openSource, openTo lists; tangled row; Now teaser (updated date + building[0] from lib/content/now.ts) linking /now; contact block (email, socials, single CV link /cv). 
Delete the old page's imports chain: remove `components/sections/`, `components/motion/`, `components/three/`, `components/ui/`, `lib/scene/`, `lib/motion/`, `lib/navigation/`, `lib/design-system/`, `lib/data/` IF AND ONLY IF nothing else still imports them (check `app/api/` and `scripts/` first; `lib/github/` stays). Old globals.css: strip dead custom classes tied to removed components, keep resets/base.

## 3. /work — app/work/page.tsx
Tier 1: cards (name, StatusBadge, tagline, 1–2 headline metrics, link to /work/[slug]). Tier 2: compact rows (name, StatusBadge, tagline, repo link). Metadata title "Work".

## 4. /work/[slug] — app/work/[slug]/page.tsx
`generateStaticParams` from tier-1 slugs only (4 pages). 404 via `notFound()` for anything else. Layout: h1 name + StatusBadge + tagline; links row (repo / demo / adapter when present); sections rendered ONLY when the field is non-empty: Problem, Constraints, Architecture, War stories (list); always: Results (MetricsTable) and stack list. OMNI page: the ember RESEARCH IN PROGRESS label directly under the h1. `generateMetadata` per slug: title = project name, description = tagline.

## 5. Remaining routes
- `app/about/page.tsx`: identity narrative from content — role, education, location/visa, affiliations, openSource, openTo, socials. No biography fluff; list-driven.
- `app/now/page.tsx`: renders lib/content/now.ts — updated date shown honestly ("Updated 2026-07-03"), building/reading/playing lists, body.
- `app/cv/page.tsx`: one link to /cv/muhammad-afsah-cv.pdf ("General CV — PDF") + note that role-specific variants are available on request. DELETE the other three PDFs from public/cv (keep muhammad-afsah-cv.pdf only).
- `app/not-found.tsx`: REPLACE — in-voice 404: mono overline `// 404`, line "This route was never trained on.", link home. No banned vocabulary.
- `app/sitemap.ts`: emit /, /work, the 4 case-study URLs, /about, /now, /cv (writing added later). `app/robots.ts`: allow all.
- `app/opengraph-image.tsx`: REPLACE content with minimal void-black card: lockup in Space Grotesk + positioning line in dim + bone rule. Remove all old copy (it contains banned vocabulary).

## Hard constraints
- Banned words in any shipped file: "sovereign", "secure enclave", "SYS.INIT", "architect" as self-title. The old files you delete contain them — deleting is the point.
- OMNI never "runs". Verified-only `signal` rule enforced in MetricsTable.
- No new dependencies. No "use client" in this task. No dangerouslySetInnerHTML.
- Finish: `npx tsc --noEmit`, `npm run lint`, then `npm run build` — but SKIP the prebuild snapshot if it needs network by running `npx next build` directly. All three must pass. If `next build` fails on the api route or something outside your scope, report it, don't hack around it.
- Report: files created/modified/deleted, check results, and confirm zero banned-vocab matches via: rg -i "sovereign|SYS\.INIT|secure enclave" app components lib content
